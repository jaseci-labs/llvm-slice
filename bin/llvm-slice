#!/usr/bin/env bash
# Thin wrapper around the Jac client CLI. Runs from the repo root so the
# `llvm_slice.*` module imports resolve. Requires `jac` on PATH.
set -euo pipefail
here="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$here"
exec jac run llvm_slice/cli.jac "$@"
